// 是否是超级管理员的码值
export enum Super {
    is = 1,
    not = 0,
}

// 禁用启用的码值
export enum Status {
    disable = 0,
    enable = 1,
}

// tab栏下拉框的枚举
export enum TabCommand {
    clearOther = 'clearOther', // 关闭其他
    clearAll = 'clearAll', // 关闭所有
}
